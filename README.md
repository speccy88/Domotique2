# Domotique2


```
Step Raspberry Pi

sudo apt-get install vim
sudo apt-get update
sudo apt-get install python3-zmq

sudo apt-get install couchdb
sudo vim /etc/couchdb/default.ini (bind address 0.0.0.0)
sudo systemctl restart couchdb
sudo pip3 install couchdb

Vimrc
set ts=4
set expandtab
syntax on
set background=dark
